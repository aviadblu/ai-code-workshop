---
phase: 05-tactical-map
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - client/src/components/TacticalMap.tsx
  - client/src/components/TacticalMap.test.tsx
autonomous: true
requirements:
  - MAP-01
  - MAP-02

must_haves:
  truths:
    - "Every unit in the store is drawn as a filled arc of radius 1 (2px diameter dot)"
    - "Alpha units use fillStyle #3b82f6"
    - "Bravo units use fillStyle #ef4444"
    - "Destroyed units use fillStyle #6b7280 regardless of team"
    - "ctx.beginPath() is called before every ctx.arc() call — no path accumulation"
    - "Unit coordinates (0-1000) are scaled to canvas pixel coordinates each frame"
    - "MAP-02 Vitest tests pass green"
  artifacts:
    - path: "client/src/components/TacticalMap.tsx"
      provides: "Canvas component with complete per-unit dot rendering loop"
      exports: ["default TacticalMap", "COLOURS"]
    - path: "client/src/components/TacticalMap.test.tsx"
      provides: "MAP-02 tests for colour-coded dot rendering"
      exports: []
  key_links:
    - from: "client/src/components/TacticalMap.tsx (draw loop)"
      to: "unit.team / unit.status"
      via: "COLOURS lookup: status==='destroyed' ? COLOURS.destroyed : COLOURS[unit.team]"
      pattern: "COLOURS\\[unit\\.team\\]"
    - from: "client/src/components/TacticalMap.tsx (draw loop)"
      to: "canvas coordinate space"
      via: "px = (unit.x / 1000) * canvas.width"
      pattern: "unit\\.x / 1000"
---

<objective>
Add per-unit dot rendering to the rAF draw loop: a COLOURS constant, coordinate mapping from unit space (0-1000) to canvas pixels, and a `ctx.beginPath() + ctx.arc() + ctx.fill()` call per unit.

Purpose: Renders all 20k units as colour-coded 2px dots — the core visual output of the tactical map.
Output: Updated `TacticalMap.tsx` with dot rendering; updated test file with MAP-02 tests; all MAP-01 + MAP-02 tests green.
</objective>

<execution_context>
@/Users/aviad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aviad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-tactical-map/05-RESEARCH.md
@.planning/phases/05-tactical-map/05-01-SUMMARY.md

@client/src/components/TacticalMap.tsx
@client/src/components/TacticalMap.test.tsx
@client/src/types.ts
</context>

<interfaces>
<!-- Contracts already established by plan 05-01. -->

COLOURS constant to add (exact values locked in requirements and spec):
```typescript
const COLOURS = {
  alpha: '#3b82f6',
  bravo: '#ef4444',
  destroyed: '#6b7280',
} as const
```

Coordinate mapping formula (unit space 0-1000 → canvas pixels):
```typescript
const px = (unit.x / 1000) * canvas.width
const py = (unit.y / 1000) * canvas.height
```

Per-unit draw sequence (order matters — beginPath before arc):
```typescript
ctx.fillStyle = colour
ctx.beginPath()
ctx.arc(px, py, 1, 0, Math.PI * 2)
ctx.fill()
```

Do NOT use: `[...units.values()]` — spreads allocate a 20k-element array every frame.
Use: `for (const unit of units.values())` — iterates the Map directly.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MAP-02 failing tests (RED)</name>
  <files>client/src/components/TacticalMap.test.tsx</files>
  <read_first>
    - client/src/components/TacticalMap.test.tsx — existing MAP-01 tests; append MAP-02 tests to the same file
    - .planning/phases/05-tactical-map/05-RESEARCH.md — Pattern 3 (per-unit dot rendering), Pitfall 4 (beginPath missing)
    - client/src/types.ts — Unit interface for constructing test fixtures
  </read_first>
  <behavior>
    - MAP-02-a: An Alpha living unit causes ctx.fillStyle to be set to '#3b82f6' and ctx.arc to be called with the correct scaled coordinates
    - MAP-02-b: A Bravo living unit causes ctx.fillStyle to be set to '#ef4444'
    - MAP-02-c: A destroyed unit (any team) causes ctx.fillStyle to be set to '#6b7280'
    - MAP-02-d: ctx.beginPath() is called once per unit drawn (not once globally)
    - MAP-02-e: With 3 units in the store, ctx.arc is called exactly 3 times per rAF frame
    - MAP-02-f: Coordinates are scaled: unit at x=500, y=500 on a 1000×1000 canvas draws arc at (500, 500); unit at x=0, y=1000 draws arc at (0, 1000)
  </behavior>
  <action>
    Append a new `describe('MAP-02: unit dot colour coding')` block to `client/src/components/TacticalMap.test.tsx`.

    Test setup for MAP-02 tests:
    - Set canvas.width = 1000 and canvas.height = 1000 on the canvas element before triggering rAF (use Object.defineProperty or mock)
    - Pre-populate useUnitsStore.getState mock to return units Map with 3 test units:
      ```typescript
      const testUnits = new Map([
        ['u-001', { id: 'u-001', team: 'alpha', x: 500, y: 250, health: 80, status: 'moving' }],
        ['u-002', { id: 'u-002', team: 'bravo', x: 100, y: 100, health: 50, status: 'idle' }],
        ['u-003', { id: 'u-003', team: 'alpha', x: 0, y: 1000, health: 0, status: 'destroyed' }],
      ])
      ```

    For MAP-02-a: after triggering rAF callback, check that `mockCtx.fillStyle` was set to '#3b82f6' at some point, and `mockCtx.arc` was called with `(500, 250, 1, 0, Math.PI * 2)`.

    For MAP-02-d: assert `mockCtx.beginPath.mock.calls.length === 3`.
    For MAP-02-e: assert `mockCtx.arc.mock.calls.length === 3`.
    For MAP-02-f: arc call for u-003 should be `(0, 1000, 1, 0, Math.PI * 2)`.

    Run: `cd /Users/aviad/code/ai-code-workshop/client && npx vitest run src/components/TacticalMap.test.tsx`
    MAP-02 tests MUST fail (RED) because dot drawing is not yet in TacticalMap.tsx.
    MAP-01 tests must still pass.
  </action>
  <verify>
    <automated>cd /Users/aviad/code/ai-code-workshop/client && npx vitest run src/components/TacticalMap.test.tsx 2>&1 | tail -25</automated>
    MAP-01 tests pass; MAP-02 tests fail. Output shows MAP-02 failure lines, not import/syntax errors.
  </verify>
  <acceptance_criteria>
    - grep: `grep -n "MAP-02" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.test.tsx` returns at least 5 lines
    - grep: `grep -n "#3b82f6" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.test.tsx` returns at least 1 line
    - grep: `grep -n "#ef4444" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.test.tsx` returns at least 1 line
    - grep: `grep -n "#6b7280" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.test.tsx` returns at least 1 line
    - Vitest run shows MAP-01 passing, MAP-02 failing
  </acceptance_criteria>
  <done>MAP-02 tests written and failing; MAP-01 tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement per-unit dot rendering (GREEN)</name>
  <files>client/src/components/TacticalMap.tsx</files>
  <read_first>
    - client/src/components/TacticalMap.test.tsx — MAP-02 expectations to satisfy
    - .planning/phases/05-tactical-map/05-RESEARCH.md — Pattern 3 (per-unit dot rendering), Anti-Patterns (no array spread, beginPath before arc)
    - client/src/components/TacticalMap.tsx — current state from plan 05-01; locate the draw loop placeholder
  </read_first>
  <behavior>
    - COLOURS const defined at module level: { alpha: '#3b82f6', bravo: '#ef4444', destroyed: '#6b7280' }
    - After ctx.clearRect(), iterate units.values() with for...of (NOT [...units.values()])
    - For each unit: colour = unit.status === 'destroyed' ? COLOURS.destroyed : COLOURS[unit.team]
    - px = (unit.x / 1000) * canvas.width; py = (unit.y / 1000) * canvas.height
    - ctx.fillStyle = colour; ctx.beginPath(); ctx.arc(px, py, 1, 0, Math.PI * 2); ctx.fill()
    - All other code from plan 05-01 (ResizeObserver, rAF scaffold, guard) is preserved unchanged
  </behavior>
  <action>
    Edit `client/src/components/TacticalMap.tsx`:

    1. Add at module level (after imports, before the component):
       ```typescript
       const COLOURS = {
         alpha: '#3b82f6',
         bravo: '#ef4444',
         destroyed: '#6b7280',
       } as const
       ```

    2. Inside the draw function, after `ctx.clearRect(0, 0, canvas.width, canvas.height)`, add the unit iteration loop:
       ```typescript
       for (const unit of units.values()) {
         const colour = unit.status === 'destroyed' ? COLOURS.destroyed : COLOURS[unit.team]
         const px = (unit.x / 1000) * canvas.width
         const py = (unit.y / 1000) * canvas.height
         ctx.fillStyle = colour
         ctx.beginPath()
         ctx.arc(px, py, 1, 0, Math.PI * 2)
         ctx.fill()
       }
       ```

    3. Do NOT spread units.values() into an array.
    4. Do NOT use ctx.save()/ctx.restore() — unnecessary overhead.
    5. Preserve all existing code from plan 05-01 unchanged.

    After editing, run the full test file to confirm MAP-01 + MAP-02 all green.
  </action>
  <verify>
    <automated>cd /Users/aviad/code/ai-code-workshop/client && npx vitest run src/components/TacticalMap.test.tsx</automated>
    All MAP-01 and MAP-02 tests pass. Exit code 0.
  </verify>
  <acceptance_criteria>
    - grep: `grep -n "COLOURS" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.tsx` returns at least 4 lines (const definition + 3 uses)
    - grep: `grep -n "#3b82f6" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.tsx` returns 1 line
    - grep: `grep -n "ctx.beginPath" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.tsx` returns 1 line (inside unit loop, one call per unit)
    - grep: `grep -n "ctx.arc" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.tsx` returns 1 line
    - grep: `grep -n "\.\.\." /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.tsx` returns 0 lines matching spread of units (no array allocation)
    - grep: `grep -n "unit\.x / 1000" /Users/aviad/code/ai-code-workshop/client/src/components/TacticalMap.tsx` returns 1 line
    - `cd /Users/aviad/code/ai-code-workshop/client && npx vitest run src/components/TacticalMap.test.tsx` exits 0
    - `cd /Users/aviad/code/ai-code-workshop/client && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All units rendered as colour-coded 2px dots; MAP-01 + MAP-02 tests all green; TypeScript clean.</done>
</task>

</tasks>

<verification>
Full client test suite:
`cd /Users/aviad/code/ai-code-workshop/client && npx vitest run`

TypeScript:
`cd /Users/aviad/code/ai-code-workshop/client && npx tsc --noEmit`
</verification>

<success_criteria>
- COLOURS constant exported/defined with exact hex values: alpha=#3b82f6, bravo=#ef4444, destroyed=#6b7280
- Per-unit loop uses `for (const unit of units.values())` — no array spread
- `ctx.beginPath()` called before each `ctx.arc()` call
- Coordinate scaling: px = (unit.x / 1000) * canvas.width
- All MAP-01 + MAP-02 Vitest tests pass
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-tactical-map/05-02-SUMMARY.md` using the summary template.
</output>
